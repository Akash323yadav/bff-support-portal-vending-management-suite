// const express = require("express");
// const cors = require("cors");

// const app = express();

// // Middlewares
// app.use(cors());
// app.use(express.json());

// // Test route (pehle ye check karo)
// app.get("/", (req, res) => {
//   res.send("Complaint Support Backend is running");
// });

// module.exports = app;
